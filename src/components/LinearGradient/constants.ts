const DEFAULT_START = { x: 0, y: 0 }
const DEFAULT_STOP = { x: 0, y: 1 }
const DEFAULT_COLORS = ['rgba(0, 0, 0, 0)', 'rgba(0, 0, 0, 0.7)']
const DEFAULT_LOCATIONS = [0, 1]

export { DEFAULT_LOCATIONS, DEFAULT_COLORS, DEFAULT_START, DEFAULT_STOP }
